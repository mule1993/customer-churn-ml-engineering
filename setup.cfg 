[metadata]
name = customer-churn-ml-engineering
version = 0.1.0
description = Customer churn ML project with full pipeline
author = Muluneh Gashaw Teklu
python_requires = >=3.10

[options]
packages = find:
package_dir =
    = src
install_requires =
    pandas>=1.5
    scikit-learn>=1.2
    xgboost>=1.7
    joblib>=1.2
    pytest>=7.0

[options.packages.find]
where = src
